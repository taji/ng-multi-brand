export interface Brand {
  id: string;
  name: string;
  primaryColor: string;
  secondaryColor: string;
  logo: string;
}

export interface BrandContent {
  title: string;
  subtitle: string;
  heroImage: string;
  ctaText: string;
}
