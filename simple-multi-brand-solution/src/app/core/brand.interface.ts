export interface Brand {
  id: string;
  name: string;
  primaryColor: string;
  secondaryColor: string;
  logo: string;
}

export interface HeroContent {
  title: string;
  subtitle: string;
  heroImage: string;
  ctaText: string;
}
